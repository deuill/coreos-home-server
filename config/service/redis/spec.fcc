variant: fcos
version: 1.3.0
storage:
  files:
    - path: /etc/systemd/system/redis.service
      contents:
        local: service/redis/redis.service
systemd:
  units:
    - name: redis.service
      enabled: true
