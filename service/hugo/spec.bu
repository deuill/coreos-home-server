variant: fcos
version: 1.3.0
storage:
  trees:
    - path: /etc/coreos-home-server/hugo
      local: service/hugo/
    - path: /etc/systemd/system
      local: service/hugo/systemd/
systemd:
  units:
    - name: hugo.service
      enabled: true
